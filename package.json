{
  "name": "@ramenha0141/tetris.mbt",
  "scripts": {
    "dev": "moon clean && moon build && vite dev",
    "build": "moon clean && moon build && vite build",
    "preview": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy"
  },
  "dependencies": {
    "tailwindcss": "^4.1.17"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.17",
    "vite": "^7.2.4",
    "wrangler": "^4.51.0"
  }
}